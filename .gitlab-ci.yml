variables:
  MAVEN_OPTS: "-Dhttps.protocols=TLSv1.2 -Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=WARN -Dorg.slf4j.simpleLogger.showDateTime=true -Djava.awt.headless=true"
  MAVEN_CLI_OPTS: "--batch-mode -ntp --errors --fail-at-end --show-version -DinstallAtEnd=true -DdeployAtEnd=true"
  SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"  # Defines the location of the analysis task cache
  GIT_DEPTH: "0"  # Tells git to fetch all the branches of the project, required by the analysis task
  GIT_STRATEGY: clone
  TZ: "Pacific/Auckland"

image: maven:3-openjdk-11

stages:
  - build
  - test
  - deploy  
  - test-staging
  - release

default:
  before_script:
    - cp $settings_xml ~/.m2/settings.xml

initial-build:
  tags:
    - docker
  stage: build
  except:
    - master
  script:
    - mvn $MAVEN_CLI_OPTS -Pjenkins-build clean deploy

include:
  - component: $CI_SERVER_FQDN/devops/components/dependency-scan@1.0.5
    inputs:
      stage: test

integration-tests:
  tags:
    - docker
  stage: test
  script:
    - mvn $MAVEN_CLI_OPTS integration-test
  artifacts:
    paths:
      - public
    expire_in: 1 week
    reports:
      junit:
        - fred-main-webapp/target/surefire-reports/TEST-*.xml
        - fred-acceptance-tests/target/failsafe-reports/TEST-*.xml

release:
  tags:
    - docker
  stage: release
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_COMMIT_REF_NAME == "master"
  script:
    - git config user.name "\"$GITLAB_USER_NAME\""
    - git config user.email "\"$GITLAB_USER_EMAIL\""
    - git config http.sslverify false
    - git remote set-url --push origin "https://gitlab-project-token:$release_token@git.gns.cri.nz/$CI_PROJECT_PATH.git"
    - git config pull.ff only
    - cp $settings_xml ~/.m2/settings.xml
    - mvn $MAVEN_CLI_OPTS gitflow:release-start -Dgitflow.branch=$CI_COMMIT_REF_NAME
    #- mvn $MAVEN_CLI_OPTS -DpreReleaseGoals=sonar:sonar -Dsonar.projectKey=applications_FRED_AYTgDtwRQ_8RozZJZ79X gitflow:release-finish
    - mvn $MAVEN_CLI_OPTS gitflow:release-finish -Dgitflow.branch=$CI_COMMIT_REF_NAME

deploy-staging:
  tags:
    - docker
  stage: deploy
  script:
    - mvn $MAVEN_CLI_OPTS -DdeploymentURL=$webapp_staging_url:8005/manager/text tomcat7:redeploy
  when: manual

test-staging:
  tags:
    - docker
  stage: test-staging
  script:
    - mvn $MAVEN_CLI_OPTS clean verify -Pdevelopment
  when: manual

